'use strict'

//Factory function to create players
const player = function (mark) {
    //keeps track of marked tiles?
}

//Module
const game = {
    //Creates a game board

    //Clear the game and signal the display

    //Keeps track of marked tiles?

    //Receives input and marks a cell for the player

    //Checks for win

    //Checks for a draw
    
    //Signals display when a tile is marked
};

//Module
const display = {

    // Creates a gameboard display

    // place a symbol in a cell

    // clear the board

    // Illuminate a winning set

    // Signal game when a cell has been clicked

};